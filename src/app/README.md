# src/app/

This directory contains the application bootstrap and routing configuration.

**Note:** This is NOT Next.js app router. This is a React + Vite application.

## Files

- `App.tsx` - Main app component with providers and routing
- `routes.tsx` - Route definitions using React Router

## Convention

All feature pages are imported from `src/features/*/pages/`.
